export enum PersonaVerificationStatusEnum {
  CREATED = 'created',
  CONFIRMED = 'confirmed',
  SUBMITTED = 'submitted',
  PASSED = 'passed',
  FAILED = 'failed',
  REQUIRES_RETRY = 'requires_retry',
  CANCELLED = 'cancelled',
}
