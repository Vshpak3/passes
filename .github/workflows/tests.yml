name: Tests

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - staging
      - production

jobs:

  check-api:
    name: Check @passes/api package
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'
      - run: yarn
      - run: yarn workspace @passes/api prettier:check
      - run: yarn workspace @passes/api lint:check
      - run: yarn workspace @passes/api ts:build
      - run: yarn workspace @passes/api test

  check-ui:
    name: Check @passes/ui package
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'
      - run: yarn
      - run: yarn workspace @passes/ui prettier:check
      - run: yarn workspace @passes/ui lint:check
      - run: yarn workspace @passes/ui ts:build
      - run: yarn workspace @passes/ui build

  check-api-configs:
    name: Check @passes/api configs
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Checkout monorepo code
        uses: actions/checkout@v2
        with:
          path: monorepo
      - name: Checkout infra code
        uses: actions/checkout@v2
        with:
          repository: getpasses/infra
          ref: main
          token: ${{ secrets.INFRA_REPO_ACCESS }}
          path: infra
      - run: ./monorepo/bin/config-check.sh
